{
  "name": "cra-express",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently 'yarn start-server' 'yarn start-client'",
    "heroku-postbuild": "cd server; yarn install",
    "start": "yarn start-server",
    "start-server": "cd server; yarn start",
    "start-client": "cd client; yarn start"
  },
  "devDependencies": {
    "concurrently": "^5.1.0"
  }
}
